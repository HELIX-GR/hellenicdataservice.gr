from setuptools import setup

setup(name='helix-volume-naming-strategy',
      version='0.1',
      description='Naming for docker volumes mounted to notebooks',
      license='GPLv3',
      packages=['helix.volume_naming'],
      zip_safe=False)
